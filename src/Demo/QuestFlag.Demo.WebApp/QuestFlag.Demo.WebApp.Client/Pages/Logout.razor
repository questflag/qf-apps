@page "/logout"

<div class="flex items-center justify-center min-h-screen bg-white">
    <div class="text-center">
        <div class="inline-flex items-center justify-center w-16 h-16 rounded-2xl bg-primary text-white text-3xl font-black animate-pulse mb-8">
            QF
        </div>
        <h1 class="text-2xl font-black tracking-tighter mb-2">Signing out...</h1>
        <p class="font-semibold uppercase tracking-widest text-[10px]">Clearing your session</p>
    </div>
</div>

