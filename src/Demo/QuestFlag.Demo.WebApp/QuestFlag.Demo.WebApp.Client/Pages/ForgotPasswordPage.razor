@page "/forgot-password"

@using QuestFlag.Passport.UserClient
@inject PassportUserClient PassportClient

<PageTitle>Forgot Password â€” QuestFlag</PageTitle>

<div class="flex items-center justify-center min-h-[calc(100vh-8rem)] bg-white px-4 sm:px-6 lg:px-8">
    <div class="w-full max-w-md space-y-8 card p-8">
        <div class="text-center">
            <div class="mx-auto h-12 w-12 bg-primary text-white rounded-lg flex items-center justify-center text-xl font-bold mb-4">QF</div>
            <h1 class="text-3xl font-extrabold">Forgot Password</h1>
            <p class="mt-2 text-sm">Enter your email and we'll send a reset link</p>
        </div>

        @if (_sent)
        {
            <div class="p-4 border">
                <div class="flex">
                    <div class="ml-3">
                        <p class="text-sm font-medium">
                            âœ“ If that email is registered, a password reset link has been sent. Check your inbox.
                        </p>
                    </div>
                </div>
            </div>
            <div class="text-center mt-4">
                <a href="/login" class="text-sm font-medium text-primary hover:text-primary-hover transition-colors">Back to Sign In</a>
            </div>
        }
        else
        {
            @if (_error != null)
            {
                <div class="p-4 border">
                    <div class="flex">
                        <div class="ml-3">
                            <h3 class="text-sm font-medium">Error</h3>
                            <div class="mt-2 text-sm">
                                <p>@_error</p>
                            </div>
                        </div>
                    </div>
                </div>
            }

            <form @onsubmit="HandleSubmit" class="mt-8 space-y-6">
                <div class="space-y-4">
                    <div>
                        <label class="block text-sm font-medium mb-1">Email Address</label>
                        <input @bind="_email" type="email" placeholder="you@example.com" required class="form-control" />
                    </div>
                </div>

                <div class="pt-2">
                    <button type="submit" disabled="@_isSubmitting" class="btn btn-primary w-full text-base py-3 h-12">
                        @(_isSubmitting ? "Sendingâ€¦" : "Send Reset Link")
                    </button>
                </div>
            </form>

            <div class="text-center mt-4">
                <a href="/login" class="text-sm font-medium text-primary hover:text-primary-hover transition-colors">Back to Sign In</a>
            </div>
        }
    </div>
</div>

