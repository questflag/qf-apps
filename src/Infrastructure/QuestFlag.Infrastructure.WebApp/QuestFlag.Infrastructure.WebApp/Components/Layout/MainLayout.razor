@inherits LayoutComponentBase

<div class="flex h-screen overflow-hidden bg-gray-50">
    <!-- Sidebar for Desktop -->
    <aside class="hidden md:flex md:flex-shrink-0">
        <div class="flex flex-col w-64 border-r border-gray-200 bg-white shadow-sm">
            <div class="flex flex-col flex-grow pt-5 pb-4 overflow-y-auto">
                <NavMenu />
            </div>
        </div>
    </aside>

    <!-- Main Content -->
    <div class="flex flex-col flex-1 w-0 overflow-hidden">
        <!-- Top Navigation / Header -->
        <header class="relative z-10 flex flex-shrink-0 h-16 bg-white border-b border-gray-200 shadow-sm">
            <div class="flex justify-between px-4 sm:px-6 lg:px-8 w-full">
                <div class="flex items-center">
                    <h1 class="text-xl font-semibold text-gray-800">QuestFlag</h1>
                </div>
                <div class="flex items-center space-x-4">
                    <a href="https://learn.microsoft.com/aspnet/core/" target="_blank" class="text-sm font-medium text-gray-500 hover:text-blue-600 transition-colors">
                        Documentation
                    </a>
                </div>
            </div>
        </header>

        <!-- Page Content -->
        <main class="relative flex-1 overflow-y-auto focus:outline-none focus:ring-0">
            <div class="py-6">
                <div class="max-w-7xl mx-auto px-4 sm:px-6 md:px-8">
                    @Body
                </div>
            </div>
        </main>
    </div>
</div>

<div id="blazor-error-ui" data-nosnippet class="fixed bottom-0 left-0 right-0 z-50 hidden bg-red-50 p-4 border-t border-red-200">
    <div class="flex items-center justify-between max-w-7xl mx-auto">
        <span class="text-sm text-red-800">An unhandled error has occurred.</span>
        <div class="flex space-x-4">
            <a href="." class="reload text-sm font-medium text-red-600 hover:text-red-500">Reload</a>
            <button class="dismiss text-red-400 hover:text-red-500">
                <span class="sr-only">Dismiss</span>
                <span aria-hidden="true">&times;</span>
            </button>
        </div>
    </div>
</div>
